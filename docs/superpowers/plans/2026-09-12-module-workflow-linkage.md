# 功能关联升级实施记录

依据：../specs/2026-09-12-module-workflow-linkage.md。用户已授权自动完成升级。

状态：以下五项均已完成，详见 [验收记录](2026-09-12-module-workflow-linkage-verification.md)。

1. 关联查询：按需求、精确项目版本、迭代读取真实范围和交付关系；保留缺失提示，不新增关系存储。验证多项目同版本、多迭代、历史材料。
2. 连续导航：关系摘要、来源返回、迭代范围定位、交付来源预填。沿用现有 Ant Design 与版本工作台布局。
3. 共享安排：需求/原型来源固定，已有或新建迭代，明确预览新增/重复/其他版本。保留旧映射；用户明确选择替换时才修改。服务端检查修订、关联、权限、生命周期。
4. 交付续办：当前标签页按仓库保存输入，返回重新检查；创建仍走现有冻结与通知流程。
5. 验证：核心与 API 回归、全量 node --test、Web 构建、隔离仓库浏览器 A1–A12 场景及桌面/小屏检查。记录实际结果，不执行正式发布或真实通知。

现有业务规则不统一迁移。范围并发保护采用服务端内容修订与同步写入锁；保留被其他版本占用的范围，显式处理。
