# Flowlark 0.8.0 验证记录

日期：2026-09-10。所有操作使用隔离测试仓库，不修改实际业务需求，不调用真实外部平台写入或发送消息。

## 已完成

- 需求与归档版本的页面位置、承载范围及变更关联；已有基线锁定规则继续生效。
- 迭代候选范围安排与采用版本展示；新版归档和当前项目基线不自动替换迭代采用版本。
- 产品、研发、测试、游客沿用现有角色权限；研发与测试首页进入需求依据，详情聚合跨版本协作。
- 反馈回复、外部修改后的处理版本、按处理版本确认、开发进展与验收结果。
- 源字段与本地分析分离、差异预览与过期校验、部分失败重试、同步删除排除。
- 单条归档、软删除、恢复、关联影响提示及冻结/进行中迭代删除阻止。
- CLI 管理命令、旧索引自动重建、需求与迭代 Git 受管范围、0.8.0 版本和使用文档。

## 验证结果

| 检查 | 结果 |
|---|---|
| `node --test` | 472 项通过，0 失败 |
| 后续需求兼容性回归：`node --test test/requirement-workflow.test.js test/requirements.test.js` | 16 项通过，0 失败；包含旧索引重建和重复关联保留范围 |
| `npm run build --prefix web` | 通过，产物版本 0.8.0 |
| `node .codex-ui-regression/requirement-workflow-08.mjs` | 通过，桌面 1440px、小屏 390px、研发/测试角色 1280px |
| `git diff --check` 与新增核心文件语法检查 | 通过 |
| `node bin/flowlark.js --version`、`req --help` | 0.8.0，新增命令帮助可用 |

浏览器实际操作覆盖：需求打开原型弹窗与预览页、本地分析查看、关联已有归档及范围保存、候选迭代安排、当前采用版本不漂移、需求删除与恢复、产品评论、研发确认开发依据、测试提交验收、角色首页、远程管理按钮禁用、移动端无横向溢出。浏览器未捕获未处理异常。

构建仍有现有主包体积告警（约 1.93 MB，gzip 约 607 KB），不阻塞构建。

## 交付边界

完成的是本地源码和生产静态产物升级，没有执行 Git 发布、远端部署或对真实需求池写入。现有服务进程需要重新启动才能加载新版后端。工作区原有未提交改动保留。

需求保留旧状态枚举作为兼容接口；界面将其明确解释为原型进度。人员记录仍使用已有浏览器协作身份，并不代表企业实名认证。标注草稿转成问题会持久保存文字与坐标，截图仍属于原有本机草稿缓存。没有新增原型生产能力。
