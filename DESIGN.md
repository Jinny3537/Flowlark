# Flowlark 项目级设计说明

本文件是 Flowlark 前端与原型工作台的项目级产品说明。做导航、顶部栏、页面布局、组件文案和交互规划时，以这里和 `design-system/flowlark/MASTER.md` 为准。

## 产品定位

Flowlark 是本地原型版本库和产品协作工作台。

它服务于需要管理大量 HTML 原型迭代的产品经理、研发和测试：CLI 负责归档和同步数据，浏览器工作台负责查看原型、规格、变更、需求、迭代、交付和反馈。数据以纯文本文件存放在本地目录，并直接进入 Git。

## 当前核心对象

- 项目：产品或功能域下的一组原型版本。
- 版本：一次可追溯的 HTML 原型归档。
- 基线：当前确认用于开发或评审的版本。
- 需求：跨项目聚合业务说明、关联版本和外部链接。
- 迭代：按交付周期组织需求与版本范围。
- 交付：冻结评审材料、导出静态包并追踪团队通知。
- 反馈：围绕原型区域、规格和版本产生的评审意见。
- Git 状态：同步、冲突、只读权限和未提交变更。

## 工作台导航原则

主导航围绕工作流组织，不引入与 Flowlark 无关的行业业务域。

一级导航建议保持：

1. 个人工作台：聚合今日待处理版本、评审、交付和同步状态。
2. 项目：按产品原型查看版本。
3. 需求：按业务需求查看跨项目演进。
4. 迭代：查看本轮范围、版本清单和定稿风险。
5. 交付：查看冻结快照、导出和团队通知。
6. 草稿箱：查看导入和 AI 草稿。

操作日志、回收站和设置属于辅助入口，不应挤占核心工作流位置。

## 顶部栏功能原则

顶部栏只放跨页面、高频、需要持续感知的全局功能：

- 品牌与当前工作区：显示 Flowlark、当前仓库或工作区上下文。
- 全局搜索：支持项目、版本、需求、迭代、交付、附件和变更内容检索。
- 快速创建：导入原型、新建需求、新建迭代或创建交付快照，按当前页面上下文控制可用项。
- 待办与通知：展示评审待处理、交付通知待重试等工作台级提醒。
- Git 状态：显示冲突、未提交变更、同步入口和只读权限。
- 运行状态：显示本地服务、局域网开放、更新可用等系统状态。
- 设置入口：进入服务、Git、集成、通知、规则和外观配置。

不要把页面内动作、低频维护入口或非 Flowlark 业务概念放进顶部栏。

## 视觉与组件约束

- 使用 Vue 3、Arco Design Vue 和现有 `--fl-*` 设计 token。
- 设计 token、字阶、间距、圆角、阴影和无障碍要求以 `design-system/flowlark/MASTER.md` 为准。
- 图标统一使用 Arco Design Vue 图标或迁移期选定的单一线性图标族。
- 状态不能只靠颜色表达，必须同时有文字标签或可读说明。
- 顶部栏在窄屏下优先收起仓库路径、辅助状态和文字标签，保留搜索、状态与核心操作入口。
