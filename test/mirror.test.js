import{after,describe,test}from'node:test';import{execFileSync}from'node:child_process';import{cleanup,tmpRepo}from'./helpers.js';import{inspectMirror,refreshMirror}from'../src/core/mirror.js'
const dirs=[];after(()=>dirs.forEach(cleanup));function git(root,...args){return execFileSync('git',args,{cwd:root,encoding:'utf8',stdio:['ignore','pipe','pipe']})}
describe('只读镜像',()=>{test('没有远端时拒绝刷新',(t)=>{const root=tmpRepo();dirs.push(root);git(root,'init','-b','main');t.assert.strictEqual(inspectMirror(root).remote,null);t.assert.throws(()=>refreshMirror(root),e=>e.code==='MIRROR_REMOTE_MISSING')});test('脏工作区不会被覆盖',(t)=>{const root=tmpRepo();dirs.push(root);git(root,'init','-b','main');git(root,'remote','add','origin','https://example.com/repo.git');t.assert.throws(()=>refreshMirror(root),e=>e.code==='MIRROR_DIRTY')})})
